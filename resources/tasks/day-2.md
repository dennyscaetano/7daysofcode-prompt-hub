Day Two of Seven

The challenge for the second day is to begin building the CRUD (Create, Read, Update, Delete) functionality of a resource called `template`, as well as configuring the test environment and building the first unit tests for the application using the Jest library.

In this challenge, the data should be stored in memory, without needing to be persisted in a database such as MySQL or PostgreSQL.